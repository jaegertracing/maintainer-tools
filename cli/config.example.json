{
  "_comment": "Copy to ~/.config/maintainer-tools/config.json or pass via --config. All keys but `repos` are optional. `viewer` is omitted here — the CLI fetches it from the GraphQL `viewer` query when not set.",
  "repos": ["owner/repo-a", "owner/repo-b"],
  "maintainers": ["maintainer-one", "maintainer-two"],
  "interns": ["intern-one"],
  "codeowners": {
    "owner/repo-a": ["src/subsystem-a/**", "docs/**"],
    "owner/repo-b": ["lib/**"]
  },
  "priorityLabels": ["priority:high", "priority:medium", "priority:low"],
  "ignoreReviewRequestedOnYou": false
}
